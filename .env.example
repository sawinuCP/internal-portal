# Copy this file to .env and adjust as needed.
# SQLite file database (relative to the prisma/ folder).
DATABASE_URL="file:./dev.db"
#
# Demo users are created by the seed script (npm run db:seed) — credentials are
# documented in the README. No other secrets are required by this app.
